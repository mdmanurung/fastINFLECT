% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iteration-QC.R
\name{iteration.QC}
\alias{iteration.QC}
\title{Run unimodality quality control on metaclusterings in parallel}
\usage{
iteration.QC(
  FlowSOM.results,
  metaclustering.list,
  set.i,
  multicore = TRUE,
  cores = NULL,
  zeroes.in = FALSE,
  only.clustering.markers = TRUE,
  acquired_markers = NULL,
  uniform.test = c("both", "spread", "unimodality"),
  th.pvalue = 0.05,
  th.IQR = 2,
  verbose = FALSE,
  max.n.diptest = NULL,
  seed = 1L,
  ...
)
}
\arguments{
\item{FlowSOM.results}{A supported SOM object with completed SOM clustering. Supports \pkg{FlowSOM} objects and \pkg{kohonen} objects returned by \code{\link[kohonen]{som}} or \code{\link[kohonen]{xyf}}.}

\item{metaclustering.list}{List of metaclustering results, as generated by \link{iteration.metacluster}}

\item{set.i}{Vector containing either the desired iterations to be tested}

\item{multicore}{logical, should the QC sweep be run in parallel (fork-based \code{\link[parallel]{mclapply}} over distinct SOM-node subtrees on Unix). Ignored on Windows. Default is \code{TRUE}}

\item{cores}{If \code{multicore == TRUE}, number of cores to be used. If \code{NULL} max number of cores-1 is used.}

\item{zeroes.in}{Should be values at and below \code{0} be included. Recommended default for mass cytometry data is \code{FALSE}}

\item{only.clustering.markers}{If \code{TRUE} only evaluates markers specified as clustering markers. For \pkg{kohonen} objects this is the first data layer.}

\item{acquired_markers}{Vector of column names with marker data to be evaluated by fastINFLECT. Ignored if \code{only.clustering.markers == TRUE}}

\item{uniform.test}{What tests are performed per marker per cluster. Options are "both", "spread" , or "unimodality" as a string.}

\item{th.pvalue}{Threshold for rejecting Unimodality dip.test result. Default is \code{0.05}. For more information see \link[diptest]{dip.test}}

\item{th.IQR}{Threshold for rejecting marker distribution based on inter-quartile range. Default is arc-sinh transformed value of \code{2}.}

\item{verbose}{\code{logical} , default is \code{FALSE}}

\item{max.n.diptest}{Optional integer. If set, each (cluster, marker) sample is randomly subsampled to at most this many events before the dip test. This removes the sample-size sensitivity of the dip test (large clusters otherwise make it reject unimodality for trivial deviations) at the cost of direct legacy-score comparability. Subsampling is seeded and therefore deterministic. Default \code{NULL} preserves the legacy scoring path.}

\item{seed}{Integer base seed used for the optional subsampling. Default \code{1L}.}

\item{...}{Additional arguments to pass to \code{\link[diptest]{dip.test}} through \code{\link{FlowSOMQC}}.}
}
\value{
A \code{list} with 2 items. The first is the unimodality scores stored in U.set. Second is a \code{list} with a \code{logical} matrix for each metaclustering result.
}
\description{
Unimodality scores are computed for all metaclusterings in
\code{set.i}. Each distinct SOM-node subtree is evaluated once; the per-k
accuracy matrices are assembled from that cache. The marker-level pass/fail
definition is aligned with the original \code{FlowSOMQC()} method.
}
\seealso{
\code{\link{INFLECT}}, \code{\link{iteration.metacluster}}, \code{\link{FlowSOMQC}}
}
